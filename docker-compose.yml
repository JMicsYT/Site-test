services:
  web:
    build: .
    command: gunicorn shoshop.wsgi:application --bind 0.0.0.0:8000
    volumes:
      - ./backend:/app
      - static_volume:/app/staticfiles
      - media_volume:/app/media
    env_file:
      - .env
    depends_on:
      - db
      - redis
    restart: unless-stopped

  db:
    image: postgres:16
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-shoshop}
      POSTGRES_USER: ${POSTGRES_USER:-shoshop}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-shoshop}
    volumes:
      - db_data:/var/lib/postgresql/data
    restart: unless-stopped

  redis:
    image: redis:7
    restart: unless-stopped

  celery:
    build: .
    command: celery -A shoshop worker -l info
    volumes:
      - ./backend:/app
    env_file:
      - .env
    depends_on:
      - redis
      - db
    restart: unless-stopped

  celery-beat:
    build: .
    command: celery -A shoshop beat -l info
    volumes:
      - ./backend:/app
    env_file:
      - .env
    depends_on:
      - redis
      - db
    restart: unless-stopped

  nginx:
    image: nginx:1.25
    ports:
      - "80:80"
      # HTTPS: раскомментируйте 443, certs и shoshop.conf — см. docs/DEPLOY_GUIDE.md §6.5.1
      # - "443:443"
    volumes:
      # HTTP по IP: нужен файл config/nginx/shoshop.http.conf + в .env DJANGO_SECURE_SSL_REDIRECT=false
      - ./config/nginx/shoshop.http.conf:/etc/nginx/conf.d/default.conf:ro
      # - ./config/nginx/certs:/etc/nginx/certs:ro
      - static_volume:/var/www/static:ro
      - media_volume:/var/www/media:ro
    depends_on:
      - web
    restart: unless-stopped

volumes:
  db_data:
  static_volume:
  media_volume:

