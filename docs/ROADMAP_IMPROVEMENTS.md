# План доработок ShoShop

Список того, что можно улучшить, с приоритетами. Можно брать пункты по мере необходимости.

---

## Высокий приоритет (безопасность и надёжность)

| # | Задача | Описание | Статус |
|---|--------|----------|--------|
| 1 | **env.example** | Файл `backend/env.example` со всеми переменными и комментариями. | ✅ Сделано |
| 2 | **Редирект после входа (`?next=`)** | Поддержка параметра `next` при логине (с проверкой внутреннего URL). | ✅ Сделано |
| 3 | **Сброс пароля (email)** | В проде — реальный SMTP. Документация + настройка в settings по `EMAIL_*`. | ✅ Сделано |
| 4 | **Ограничение по email_verified** | Не давать оформлять заказы/добавлять в корзину без подтверждения email; кнопка «Отправить письмо повторно». | ✅ Сделано |

---

## Средний приоритет (функциональность и UX)

| # | Задача | Описание | Статус |
|---|--------|----------|--------|
| 5 | **Удаление из корзины** | На странице корзины — кнопка «Удалить», изменение количества (поле + OK). | ✅ Сделано |
| 6 | **Пагинация каталога** | 24 товара на страницу, ссылки «Назад» / «Вперёд», сохранение фильтров в URL. | ✅ Сделано |
| 7 | **Пагинация API каталога** | В DRF ProductViewSet включена PageNumberPagination (24 на страницу). | ✅ Сделано |
| 8 | **Страница «Мои заказы»** | В ЛК уже есть блок «История заказов»; отдельная страница при желании. | ✅ Сделано |
| 9 | **Скопировать выданный товар** | В ЛК у купленных цифровых товаров кнопка «Скопировать» (в буфер обмена). | ✅ Сделано |
| 10 | **Подтверждение при выходе** | При клике «Выйти» — диалог «Вы уверены, что хотите выйти?». | ✅ Сделано |
| 11 | **Хлебные крошки на сайте** | Главная → Каталог; Каталог → [Категория] → Товар; Главная → Корзина. | ✅ Сделано |

---

## Тесты и качество кода

| # | Задача | Описание | Статус |
|---|--------|----------|--------|
| 12 | **Тесты accounts** | Модуль `apps/accounts/tests.py`: регистрация, вход, выход, сброс пароля (мок email), верификация email. | ✅ Сделано |
| 13 | **Тесты dashboard** | Проверка доступа (только staff), основные действия: создание категории, товара, смена статуса заказа. | ✅ Сделано |
| 14 | **Тесты payments callback** | Проверка: без секрета (если задан) — 403; с секретом — 200; идемпотентность (повторный success не дублирует выдачу). | ✅ Сделано |
| 15 | **Валидация форм в админке** | Явная валидация slug (уникальность, формат), цены ≥ 0, обязательные поля в формах создания товара/категории. | ✅ Сделано |

---

## Админка и модерация

| # | Задача | Описание |
|---|--------|----------|
| 16 | **Модерация отзывов** | В админке отзывов: кнопки «Скрыть» / «Одобрить» или поле «статус» (на модерации / опубликован / скрыт), фильтр по статусу. |
| 17 | **Экспорт заказов** | Кнопка «Экспорт в CSV» на странице заказов в dashboard с фильтрами по дате/статусу. |
| 18 | **Логирование действий в админке** | Кто и когда изменил заказ/товар (через Django admin log или свой SecurityEvent для кастомной админки). |

---

## Производительность и инфраструктура

| # | Задача | Описание |
|---|--------|----------|
| 19 | **Кеш каталога** | Уже есть кеш популярных товаров на главной. При желании — кешировать список категорий и количество товаров по категориям. |
| 20 | **Статика в проде** | Проверить, что `collectstatic` вызывается при деплое и nginx раздаёт `/static/` из нужной папки. |
| 21 | **Логирование** | Настроить логирование в файл или stdout (LOGGING в settings), уровень INFO для приложения, WARNING для сторонних библиотек. |
| 22 | **Health-check** | Эндпоинт вида `/health/` или `/ping/` (проверка БД, опционально Redis) для мониторинга и оркестрации. |

---

## Визуал и контент

| # | Задача | Описание |
|---|--------|----------|
| 23 | **Единые сообщения об ошибках** | Все формы: вывод `{{ form.errors }}` или под полем в одном стиле (например, красный текст под полем). |
| 24 | **Страница 500** | Шаблон 500 с нейтральным текстом («Временная ошибка»), без технических деталей в проде. |
| 25 | **Мета-теги и SEO** | В base.html: описание и ключевые слова для главной и каталога (block meta). |
| 26 | **Favicon** | Добавить favicon.ico в static и ссылку в base.html. |

---

## Документация и деплой

| # | Задача | Описание |
|---|--------|----------|
| 27 | **Обновить README** | Указать наличие RUN_LOCAL.md, SECURITY_AND_AUDIT.md, ROADMAP; перечень переменных окружения (в т.ч. PAYMENT_CALLBACK_SECRET, PROJECT_NAME). |
| 28 | **Пример .env** | Создать `backend/env.example` (без секретов) и описать в RUN_LOCAL копирование в `.env`. |
| 29 | **Чек-лист перед релизом** | DEBUG=false; SECRET_KEY уникальный, не в репо; ALLOWED_HOSTS только нужные домены; HTTPS и DJANGO_SECURE_SSL_REDIRECT=true; PAYMENT_CALLBACK_SECRET задан; collectstatic выполнен; бэкапы БД настроены. |

---

## Идеи на будущее

- Подписка на уведомления о новом товаре в категории.
- Промокоды / скидки (поле в заказе, расчёт суммы).
- Несколько платёжных провайдеров (выбор при оплате).
- Личный кабинет продавца (если платформа станет маркетплейсом с несколькими продавцами).
- Двухфакторная аутентификация (2FA).

---

Имеет смысл начинать с пунктов **1, 2, 28** (env.example, next после логина, обновление документации) — они быстрые и сразу повышают удобство и безопасность.
